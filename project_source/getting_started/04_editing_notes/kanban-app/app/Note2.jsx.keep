import React from 'react';

export default ({value, onEdit, onDelete}) => (
  <div className="note-wrapper">
    <span className="note" onDoubleClick={onEdit}>{value}</span>
    <button className="delete-note" onClick={onDelete}>x</button>
  </div>
);